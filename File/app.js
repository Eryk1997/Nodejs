var fs = require("fs");
var http = require('./openAbout')
console.log("before");

/*
fs.readFile("test.txt", "utf-8", function (error, data) {
  if (error) console.log("Error: " + error);

  console.log(data);
});
*/
http.run
console.log("after");
